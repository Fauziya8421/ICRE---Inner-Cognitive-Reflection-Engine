{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.11.13","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"dockerImageVersionId":31192,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"source":"<a href=\"https://www.kaggle.com/code/fauziya24/icre-inner-cognitive-reflection-engine?scriptVersionId=282939939\" target=\"_blank\"><img align=\"left\" alt=\"Kaggle\" title=\"Open in Kaggle\" src=\"https://kaggle.com/static/images/open-in-kaggle.svg\"></a>","metadata":{},"cell_type":"markdown"},{"cell_type":"code","source":"# ═══════════════════════════════════════════════════════════════════\n# ICRE - Inner Cognitive Reflection Engine\n# Complete Multi-Agent System with Google Gemini API\n# Kaggle Notebook - Full Implementation\n# ═══════════════════════════════════════════════════════════════════\n\n\"\"\"\nCAPSTONE PROJECT: ICRE (Freestyle Track)\nProblem: People struggle to understand their own thought patterns without advice\nSolution: Multi-agent reflective intelligence system\n\nARCHITECTURE:\n- Agent 1: Thought Parser (extracts cognitive structure)\n- Agent 2: Cognitive Pattern Classifier (detects patterns via memory)\n- Agent 3: Reflection Engine (generates insights)\n- Agent 4: Clarity Structuring Agent (creates structured output)\n\nCAPSTONE REQUIREMENTS COVERED:\n✓ Multi-agent system (4 sequential agents)\n✓ LLM agents (Gemini)\n✓ Custom tools\n✓ Memory Bank (long-term)\n✓ Sessions (short-term)\n✓ A2A Protocol (agent-to-agent communication)\n✓ Observability (logging, metrics)\n✓ Context engineering\n\"\"\"\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 1: INSTALL DEPENDENCIES\n# ═══════════════════════════════════════════════════════════════════\n\n!pip install google-generativeai pydantic -q\n\nprint(\"✅ Dependencies installed\")\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 2: IMPORTS & CONFIGURATION\n# ═══════════════════════════════════════════════════════════════════\n\nimport google.generativeai as genai\nimport json\nimport os\nimport time\nfrom datetime import datetime\nfrom typing import Dict, List, Optional, Any\nfrom pydantic import BaseModel, Field\nfrom collections import defaultdict\nimport hashlib\n\n# Configure API (In Kaggle: Add as Secret - GOOGLE_API_KEY)\ntry:\n    from kaggle_secrets import UserSecretsClient\n    user_secrets = UserSecretsClient()\n    GOOGLE_API_KEY = user_secrets.get_secret(\"GOOGLE_API_KEY\")\nexcept:\n    # Fallback for local testing\n    GOOGLE_API_KEY = os.getenv(\"GOOGLE_API_KEY\")\n\ngenai.configure(api_key=GOOGLE_API_KEY)\n\n# Initialize Gemini model\nmodel = genai.GenerativeModel('gemini-2.5-flash')\n\nprint(\"✅ Google AI Studio configured\")\nprint(f\"📅 Timestamp: {datetime.now().isoformat()}\")\nprint(f\"🤖 Model: gemini-pro-latest\\n\")\n\n# ═══════════════════════════════════════════════════════════════════\n# HELPER: RATE LIMIT HANDLER\n# ═══════════════════════════════════════════════════════════════════\n\ndef call_with_retry(model, prompt, max_retries=5, initial_delay=2):\n    \"\"\"\n    Call Gemini API with automatic retry and exponential backoff\n    Handles rate limit errors gracefully\n    \"\"\"\n    delay = initial_delay\n    \n    for attempt in range(max_retries):\n        try:\n            response = model.generate_content(prompt)\n            return response\n        \n        except Exception as e:\n            error_str = str(e)\n            \n            # Check if it's a rate limit error\n            if \"429\" in error_str or \"ResourceExhausted\" in error_str or \"quota\" in error_str.lower():\n                if attempt < max_retries - 1:\n                    print(f\"   ⏳ Rate limit hit. Waiting {delay}s before retry (attempt {attempt + 1}/{max_retries})...\")\n                    time.sleep(delay)\n                    delay *= 2  # Exponential backoff\n                else:\n                    print(f\"   ❌ Max retries reached. Error: {error_str[:100]}\")\n                    raise\n            else:\n                # Non-rate-limit error, raise immediately\n                print(f\"   ❌ API Error: {error_str[:100]}\")\n                raise\n    \n    raise Exception(\"Failed after all retries\")\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 3: DATA MODELS (Pydantic Schemas)\n# ═══════════════════════════════════════════════════════════════════\n\nclass ThoughtInput(BaseModel):\n    \"\"\"User's raw thought input\"\"\"\n    raw_text: str\n    entry_id: str = Field(default_factory=lambda: f\"entry_{datetime.now().strftime('%Y%m%d_%H%M%S')}\")\n    timestamp: str = Field(default_factory=lambda: datetime.now().isoformat())\n\nclass ThoughtParsing(BaseModel):\n    \"\"\"Output from Agent 1: Thought Parser\"\"\"\n    primary_emotions: List[str] = Field(description=\"Main emotions detected\")\n    core_themes: List[str] = Field(description=\"Key topics/subjects\")\n    tone: str = Field(description=\"Overall emotional tone\")\n    triggers: List[str] = Field(description=\"Events/situations that prompted thought\")\n    hidden_assumptions: List[str] = Field(description=\"Implicit beliefs\")\n    intensity: int = Field(ge=1, le=10, description=\"Emotional intensity (1-10)\")\n    timestamp: str = Field(default_factory=lambda: datetime.now().isoformat())\n\nclass CognitivePattern(BaseModel):\n    \"\"\"Output from Agent 2: Pattern Classifier\"\"\"\n    recurring_emotions: List[str] = Field(description=\"Emotions that repeat across entries\")\n    recurring_triggers: List[str] = Field(description=\"Triggers that repeat\")\n    mental_loops: List[str] = Field(description=\"Circular thought patterns\")\n    pattern_frequency: Dict[str, int] = Field(description=\"How often patterns appear\")\n    emotional_trajectory: str = Field(description=\"How emotions evolve over time\")\n    similarity_score: float = Field(ge=0, le=1, description=\"Similarity to past entries\")\n\nclass ReflectionInsight(BaseModel):\n    \"\"\"Output from Agent 3: Reflection Engine\"\"\"\n    core_reflection: str = Field(description=\"Main reflective insight\")\n    underlying_tension: str = Field(description=\"Core internal conflict\")\n    cognitive_observation: str = Field(description=\"Pattern observation\")\n    no_advice_check: bool = Field(description=\"Ensures no prescriptive advice given\")\n\nclass ClarityMap(BaseModel):\n    \"\"\"Output from Agent 4: Clarity Structuring Agent\"\"\"\n    root_emotion: str\n    conflict_summary: str\n    underlying_need: str\n    mental_loop: Optional[str]\n    pattern_frequency: str\n    progress_indicator: str\n    full_reflection: str\n    entry_id: str\n    timestamp: str\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 4: MEMORY BANK (Long-term Storage)\n# ═══════════════════════════════════════════════════════════════════\n\nclass MemoryBank:\n    \"\"\"\n    Long-term memory storage for cognitive patterns\n    Implements: Context engineering, pattern detection\n    \"\"\"\n    \n    def __init__(self):\n        self.entries = []  # All thought entries\n        self.parsings = []  # All parsed data\n        self.patterns = []  # Detected patterns\n        self.reflections = []  # Generated reflections\n        \n    def store_entry(self, entry: ThoughtInput, parsing: ThoughtParsing):\n        \"\"\"Store new entry with parsing\"\"\"\n        self.entries.append(entry.model_dump())\n        self.parsings.append(parsing.model_dump())\n        \n    def store_pattern(self, pattern: CognitivePattern):\n        \"\"\"Store detected pattern\"\"\"\n        self.patterns.append(pattern.model_dump())\n        \n    def store_reflection(self, reflection: ReflectionInsight, clarity: ClarityMap):\n        \"\"\"Store reflection and clarity map\"\"\"\n        self.reflections.append({\n            \"reflection\": reflection.model_dump(),\n            \"clarity_map\": clarity.model_dump()\n        })\n    \n    def get_recent_entries(self, n: int = 5) -> List[Dict]:\n        \"\"\"Retrieve recent entries for context\"\"\"\n        return self.parsings[-n:] if len(self.parsings) >= n else self.parsings\n    \n    def get_emotion_history(self) -> List[str]:\n        \"\"\"Get all emotions from history\"\"\"\n        emotions = []\n        for parsing in self.parsings:\n            emotions.extend(parsing['primary_emotions'])\n        return emotions\n    \n    def get_trigger_history(self) -> List[str]:\n        \"\"\"Get all triggers from history\"\"\"\n        triggers = []\n        for parsing in self.parsings:\n            triggers.extend(parsing['triggers'])\n        return triggers\n    \n    def calculate_similarity(self, current_emotions: List[str], current_themes: List[str]) -> float:\n        \"\"\"Calculate similarity to historical entries\"\"\"\n        if not self.parsings:\n            return 0.0\n        \n        historical_emotions = set(self.get_emotion_history())\n        historical_themes = set()\n        for parsing in self.parsings:\n            historical_themes.update(parsing['core_themes'])\n        \n        current_emotions_set = set(current_emotions)\n        current_themes_set = set(current_themes)\n        \n        emotion_overlap = len(current_emotions_set & historical_emotions) / max(len(current_emotions_set), 1)\n        theme_overlap = len(current_themes_set & historical_themes) / max(len(current_themes_set), 1)\n        \n        return (emotion_overlap + theme_overlap) / 2\n    \n    def get_stats(self) -> Dict:\n        \"\"\"Get memory bank statistics\"\"\"\n        return {\n            \"total_entries\": len(self.entries),\n            \"total_patterns\": len(self.patterns),\n            \"total_reflections\": len(self.reflections)\n        }\n\n# Initialize global memory bank\nmemory_bank = MemoryBank()\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 5: AGENT 1 - THOUGHT PARSER AGENT\n# ═══════════════════════════════════════════════════════════════════\n\nclass ThoughtParserAgent:\n    \"\"\"\n    Agent 1: Extracts structured cognitive data from raw thoughts\n    Uses: LLM (Gemini), Custom parsing tool, Sequential processing\n    \"\"\"\n    \n    def __init__(self, model):\n        self.model = model\n        self.session_log = []\n        \n    def parse_thought(self, thought_input: ThoughtInput) -> ThoughtParsing:\n        \"\"\"Parse raw thought into structured cognitive data\"\"\"\n        \n        prompt = f\"\"\"You are a Thought Parser Agent in the ICRE system.\n\nExtract cognitive structure from this thought. DO NOT give advice, suggestions, or solutions.\n\nONLY identify and return valid JSON:\n{{\n  \"primary_emotions\": [\"emotion1\", \"emotion2\"],\n  \"core_themes\": [\"theme1\", \"theme2\"],\n  \"tone\": \"descriptive tone\",\n  \"triggers\": [\"trigger1\"],\n  \"hidden_assumptions\": [\"assumption1\"],\n  \"intensity\": 7\n}}\n\nThought to parse:\n{thought_input.raw_text}\n\nRespond ONLY with valid JSON, no markdown, no explanations.\"\"\"\n\n        try:\n            response = call_with_retry(self.model, prompt)\n            raw_text = response.text.strip()\n            \n            # Clean JSON\n            if \"```json\" in raw_text:\n                raw_text = raw_text.split(\"```json\")[1].split(\"```\")[0].strip()\n            elif \"```\" in raw_text:\n                raw_text = raw_text.split(\"```\")[1].split(\"```\")[0].strip()\n            \n            parsed_data = json.loads(raw_text)\n            parsed_data[\"timestamp\"] = datetime.now().isoformat()\n            \n            result = ThoughtParsing(**parsed_data)\n            \n            # Log for observability\n            self.session_log.append({\n                \"agent\": \"ThoughtParser\",\n                \"entry_id\": thought_input.entry_id,\n                \"status\": \"success\",\n                \"timestamp\": datetime.now().isoformat()\n            })\n            \n            return result\n            \n        except Exception as e:\n            self.session_log.append({\n                \"agent\": \"ThoughtParser\",\n                \"entry_id\": thought_input.entry_id,\n                \"status\": \"error\",\n                \"error\": str(e),\n                \"timestamp\": datetime.now().isoformat()\n            })\n            raise\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 6: AGENT 2 - COGNITIVE PATTERN CLASSIFIER\n# ═══════════════════════════════════════════════════════════════════\n\nclass CognitivePatternAgent:\n    \"\"\"\n    Agent 2: Detects patterns using Memory Bank\n    Uses: Memory Bank, A2A communication, Pattern detection\n    \"\"\"\n    \n    def __init__(self, model, memory_bank: MemoryBank):\n        self.model = model\n        self.memory_bank = memory_bank\n        self.session_log = []\n    \n    def detect_patterns(self, current_parsing: ThoughtParsing) -> CognitivePattern:\n        \"\"\"Detect cognitive patterns by comparing with memory\"\"\"\n        \n        # Retrieve historical context (context engineering)\n        recent_entries = self.memory_bank.get_recent_entries(5)\n        emotion_history = self.memory_bank.get_emotion_history()\n        trigger_history = self.memory_bank.get_trigger_history()\n        \n        # Calculate similarity score\n        similarity = self.memory_bank.calculate_similarity(\n            current_parsing.primary_emotions,\n            current_parsing.core_themes\n        )\n        \n        # Count pattern frequencies\n        emotion_counts = defaultdict(int)\n        for emotion in emotion_history:\n            emotion_counts[emotion] += 1\n        \n        trigger_counts = defaultdict(int)\n        for trigger in trigger_history:\n            trigger_counts[trigger] += 1\n        \n        # Build context for LLM\n        context = f\"\"\"Recent emotional patterns: {list(emotion_counts.keys())[:5]}\nRecent triggers: {list(trigger_counts.keys())[:5]}\nCurrent emotions: {current_parsing.primary_emotions}\nCurrent triggers: {current_parsing.triggers}\nHistorical entries: {len(recent_entries)}\"\"\"\n\n        prompt = f\"\"\"You are a Cognitive Pattern Classifier Agent.\n\nAnalyze this cognitive data against historical patterns:\n\n{context}\n\nIdentify:\n1. Which emotions are recurring\n2. Which triggers are recurring\n3. Any mental loops (circular thinking patterns)\n4. Emotional trajectory (improving/declining/stable)\n\nRespond ONLY with valid JSON:\n{{\n  \"recurring_emotions\": [\"emotion1\"],\n  \"recurring_triggers\": [\"trigger1\"],\n  \"mental_loops\": [\"loop description\"],\n  \"pattern_frequency\": {{\"emotion1\": 3}},\n  \"emotional_trajectory\": \"description\",\n  \"similarity_score\": 0.0\n}}\"\"\"\n\n        try:\n            response = call_with_retry(self.model, prompt)\n            raw_text = response.text.strip()\n            \n            if \"```json\" in raw_text:\n                raw_text = raw_text.split(\"```json\")[1].split(\"```\")[0].strip()\n            elif \"```\" in raw_text:\n                raw_text = raw_text.split(\"```\")[1].split(\"```\")[0].strip()\n            \n            parsed_data = json.loads(raw_text)\n            parsed_data[\"similarity_score\"] = similarity\n            \n            result = CognitivePattern(**parsed_data)\n            \n            # Store in memory bank\n            self.memory_bank.store_pattern(result)\n            \n            # A2A communication log\n            self.session_log.append({\n                \"agent\": \"PatternClassifier\",\n                \"a2a_message\": f\"Detected {len(result.recurring_emotions)} recurring emotions\",\n                \"status\": \"success\",\n                \"timestamp\": datetime.now().isoformat()\n            })\n            \n            return result\n            \n        except Exception as e:\n            self.session_log.append({\n                \"agent\": \"PatternClassifier\",\n                \"status\": \"error\",\n                \"error\": str(e),\n                \"timestamp\": datetime.now().isoformat()\n            })\n            raise\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 7: AGENT 3 - REFLECTION ENGINE\n# ═══════════════════════════════════════════════════════════════════\n\nclass ReflectionEngineAgent:\n    \"\"\"\n    Agent 3: Generates deep reflection (NO ADVICE)\n    Uses: LLM, Context compaction, Non-prescriptive insight\n    \"\"\"\n    \n    def __init__(self, model):\n        self.model = model\n        self.session_log = []\n    \n    def generate_reflection(self, \n                          parsing: ThoughtParsing, \n                          pattern: CognitivePattern) -> ReflectionInsight:\n        \"\"\"Generate reflective insight without advice\"\"\"\n        \n        # Context compaction - summarize key elements\n        context = f\"\"\"\nEmotions: {', '.join(parsing.primary_emotions)}\nThemes: {', '.join(parsing.core_themes)}\nIntensity: {parsing.intensity}/10\nRecurring patterns: {', '.join(pattern.recurring_emotions)}\nMental loops: {', '.join(pattern.mental_loops) if pattern.mental_loops else 'None detected'}\n\"\"\"\n\n        prompt = f\"\"\"You are a Reflection Engine Agent in ICRE.\n\nGenerate a reflective observation about this person's cognitive state. \n\nCRITICAL RULES:\n- NO ADVICE (don't say \"you should\", \"try to\", \"consider\")\n- NO SUGGESTIONS (don't prescribe actions)\n- ONLY REFLECT (mirror back what you observe)\n\nContext:\n{context}\n\nProvide valid JSON:\n{{\n  \"core_reflection\": \"One deep observation about their mental state\",\n  \"underlying_tension\": \"The core internal conflict you observe\",\n  \"cognitive_observation\": \"A pattern you notice in their thinking\",\n  \"no_advice_check\": true\n}}\n\nBe empathetic, precise, and purely observational.\"\"\"\n\n        try:\n            response = call_with_retry(self.model, prompt)\n            raw_text = response.text.strip()\n            \n            if \"```json\" in raw_text:\n                raw_text = raw_text.split(\"```json\")[1].split(\"```\")[0].strip()\n            elif \"```\" in raw_text:\n                raw_text = raw_text.split(\"```\")[1].split(\"```\")[0].strip()\n            \n            parsed_data = json.loads(raw_text)\n            result = ReflectionInsight(**parsed_data)\n            \n            self.session_log.append({\n                \"agent\": \"ReflectionEngine\",\n                \"advice_check_passed\": result.no_advice_check,\n                \"status\": \"success\",\n                \"timestamp\": datetime.now().isoformat()\n            })\n            \n            return result\n            \n        except Exception as e:\n            self.session_log.append({\n                \"agent\": \"ReflectionEngine\",\n                \"status\": \"error\",\n                \"error\": str(e),\n                \"timestamp\": datetime.now().isoformat()\n            })\n            raise\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 8: AGENT 4 - CLARITY STRUCTURING AGENT\n# ═══════════════════════════════════════════════════════════════════\n\nclass ClarityStructuringAgent:\n    \"\"\"\n    Agent 4: Creates final Clarity Map\n    Uses: Structured output, Synthesis, A2A final communication\n    \"\"\"\n    \n    def __init__(self):\n        self.session_log = []\n    \n    def create_clarity_map(self,\n                          thought_input: ThoughtInput,\n                          parsing: ThoughtParsing,\n                          pattern: CognitivePattern,\n                          reflection: ReflectionInsight) -> ClarityMap:\n        \"\"\"Synthesize all agent outputs into Clarity Map\"\"\"\n        \n        # Determine root emotion (highest intensity recurring emotion)\n        root_emotion = parsing.primary_emotions[0] if parsing.primary_emotions else \"unclear\"\n        \n        # Progress indicator based on pattern similarity\n        if pattern.similarity_score > 0.7:\n            progress = \"High pattern repetition - cognitive loop present\"\n        elif pattern.similarity_score > 0.4:\n            progress = \"Moderate pattern - some recurring themes\"\n        else:\n            progress = \"New cognitive territory - emerging pattern\"\n        \n        clarity_map = ClarityMap(\n            root_emotion=root_emotion,\n            conflict_summary=reflection.underlying_tension,\n            underlying_need=reflection.core_reflection,\n            mental_loop=pattern.mental_loops[0] if pattern.mental_loops else None,\n            pattern_frequency=f\"{len(pattern.recurring_emotions)} recurring emotions, {len(pattern.recurring_triggers)} recurring triggers\",\n            progress_indicator=progress,\n            full_reflection=reflection.cognitive_observation,\n            entry_id=thought_input.entry_id,\n            timestamp=datetime.now().isoformat()\n        )\n        \n        # A2A final communication\n        self.session_log.append({\n            \"agent\": \"ClarityStructuring\",\n            \"a2a_message\": \"Clarity Map created - pipeline complete\",\n            \"entry_id\": thought_input.entry_id,\n            \"status\": \"success\",\n            \"timestamp\": datetime.now().isoformat()\n        })\n        \n        return clarity_map\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 9: ICRE ORCHESTRATOR (Multi-Agent Coordinator)\n# ═══════════════════════════════════════════════════════════════════\n\nclass ICREOrchestrator:\n    \"\"\"\n    Main orchestrator coordinating all 4 agents\n    Implements: Sequential agent flow, A2A protocol, Complete pipeline\n    \"\"\"\n    \n    def __init__(self, model, memory_bank: MemoryBank):\n        self.agent1 = ThoughtParserAgent(model)\n        self.agent2 = CognitivePatternAgent(model, memory_bank)\n        self.agent3 = ReflectionEngineAgent(model)\n        self.agent4 = ClarityStructuringAgent()\n        self.memory_bank = memory_bank\n        self.execution_log = []\n    \n    def process_thought(self, raw_thought: str) -> ClarityMap:\n        \"\"\"\n        Execute complete ICRE pipeline\n        \n        Flow: Input → Agent1 → Agent2 → Agent3 → Agent4 → Output\n        \"\"\"\n        \n        start_time = datetime.now()\n        \n        print(\"🧠 ICRE Pipeline Starting...\\n\")\n        \n        # Create thought input\n        thought_input = ThoughtInput(raw_text=raw_thought)\n        \n        # AGENT 1: Parse thought\n        print(\"⚙️  Agent 1: Thought Parser...\")\n        parsing = self.agent1.parse_thought(thought_input)\n        print(f\"   ✓ Extracted {len(parsing.primary_emotions)} emotions, {len(parsing.core_themes)} themes\")\n        time.sleep(1)  # Rate limit buffer\n        \n        # Store in memory\n        self.memory_bank.store_entry(thought_input, parsing)\n        \n        # AGENT 2: Detect patterns\n        print(\"⚙️  Agent 2: Pattern Classifier...\")\n        pattern = self.agent2.detect_patterns(parsing)\n        print(f\"   ✓ Detected {len(pattern.recurring_emotions)} recurring patterns\")\n        time.sleep(1)  # Rate limit buffer\n        \n        # AGENT 3: Generate reflection\n        print(\"⚙️  Agent 3: Reflection Engine...\")\n        reflection = self.agent3.generate_reflection(parsing, pattern)\n        print(f\"   ✓ Generated reflection (Advice-free: {reflection.no_advice_check})\")\n        time.sleep(1)  # Rate limit buffer\n        \n        # AGENT 4: Create clarity map\n        print(\"⚙️  Agent 4: Clarity Structuring...\")\n        clarity_map = self.agent4.create_clarity_map(\n            thought_input, parsing, pattern, reflection\n        )\n        print(f\"   ✓ Clarity Map created\")\n        \n        # Store complete reflection\n        self.memory_bank.store_reflection(reflection, clarity_map)\n        \n        # Calculate execution time\n        end_time = datetime.now()\n        execution_time = (end_time - start_time).total_seconds()\n        \n        # Log execution\n        self.execution_log.append({\n            \"entry_id\": thought_input.entry_id,\n            \"execution_time_seconds\": execution_time,\n            \"agents_executed\": 4,\n            \"timestamp\": end_time.isoformat()\n        })\n        \n        print(f\"\\n✅ Pipeline complete in {execution_time:.2f}s\\n\")\n        \n        return clarity_map\n    \n    def display_clarity_map(self, clarity_map: ClarityMap):\n        \"\"\"Pretty print the Clarity Map\"\"\"\n        print(\"=\"*70)\n        print(\"🗺️  CLARITY MAP\")\n        print(\"=\"*70)\n        print(f\"\\n🎯 Root Emotion: {clarity_map.root_emotion}\")\n        print(f\"\\n⚡ Core Conflict:\\n   {clarity_map.conflict_summary}\")\n        print(f\"\\n💭 Underlying Need:\\n   {clarity_map.underlying_need}\")\n        \n        if clarity_map.mental_loop:\n            print(f\"\\n🔄 Mental Loop Detected:\\n   {clarity_map.mental_loop}\")\n        \n        print(f\"\\n📊 Pattern Frequency: {clarity_map.pattern_frequency}\")\n        print(f\"\\n📈 Progress: {clarity_map.progress_indicator}\")\n        print(f\"\\n🧠 Cognitive Observation:\\n   {clarity_map.full_reflection}\")\n        print(f\"\\n🆔 Entry ID: {clarity_map.entry_id}\")\n        print(f\"⏰ Timestamp: {clarity_map.timestamp}\")\n        print(\"=\"*70 + \"\\n\")\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 10: TESTING THE COMPLETE SYSTEM\n# ═══════════════════════════════════════════════════════════════════\n\nprint(\"\\n\" + \"=\"*70)\nprint(\"🧪 TESTING COMPLETE ICRE SYSTEM\")\nprint(\"=\"*70 + \"\\n\")\n\n# Initialize orchestrator\nicre = ICREOrchestrator(model, memory_bank)\n\n# TEST CASE 1: Work-related stress\nprint(\"📝 TEST CASE 1: Work-Related Stress\\n\")\nprint(\"-\"*70 + \"\\n\")\n\ntest_thought_1 = \"\"\"I've been working on this project for 3 months and it feels like \nI'm spinning my wheels. Everyone around me seems to be getting promoted or recognized, \nbut I'm stuck doing the same tasks. Maybe I'm not good enough for this role. I keep \nwondering if I should just quit and try something else, but then I feel like a failure \nfor even thinking that.\"\"\"\n\nclarity_1 = icre.process_thought(test_thought_1)\nicre.display_clarity_map(clarity_1)\n\n# TEST CASE 2: Relationship reflection\nprint(\"\\n\" + \"=\"*70)\nprint(\"📝 TEST CASE 2: Relationship Reflection\\n\")\nprint(\"-\"*70 + \"\\n\")\n\nprint(\"⏳ Waiting 3s before next test (rate limit protection)...\\n\")\ntime.sleep(3)\n\ntest_thought_2 = \"\"\"Had a great conversation with Sarah today. She really gets what \nI'm trying to do with my career pivot. But I noticed I was downplaying my achievements \nwhen she asked about my progress. Why do I do that? It's like I don't want to seem \ntoo proud or something. Maybe I don't actually believe I deserve the success?\"\"\"\n\nclarity_2 = icre.process_thought(test_thought_2)\nicre.display_clarity_map(clarity_2)\n\n# TEST CASE 3: Pattern detection test (should show recurring patterns)\nprint(\"\\n\" + \"=\"*70)\nprint(\"📝 TEST CASE 3: Identity Reflection\\n\")\nprint(\"-\"*70 + \"\\n\")\n\nprint(\"⏳ Waiting 3s before next test (rate limit protection)...\\n\")\ntime.sleep(3)\n\ntest_thought_3 = \"\"\"I keep comparing myself to others and feeling inadequate. It's \nexhausting. Even when things go well, I find something to criticize about myself. \nWhy can't I just accept that I'm doing okay?\"\"\"\n\nclarity_3 = icre.process_thought(test_thought_3)\nicre.display_clarity_map(clarity_3)\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 11: OBSERVABILITY & METRICS\n# ═══════════════════════════════════════════════════════════════════\n\nprint(\"\\n\" + \"=\"*70)\nprint(\"📊 OBSERVABILITY DASHBOARD\")\nprint(\"=\"*70 + \"\\n\")\n\ndef display_metrics(icre: ICREOrchestrator):\n    \"\"\"Display comprehensive system metrics\"\"\"\n    \n    # Memory Bank Stats\n    stats = memory_bank.get_stats()\n    print(f\"💾 MEMORY BANK STATISTICS\")\n    print(f\"   Total Entries: {stats['total_entries']}\")\n    print(f\"   Total Patterns Detected: {stats['total_patterns']}\")\n    print(f\"   Total Reflections: {stats['total_reflections']}\")\n    \n    # Execution Metrics\n    print(f\"\\n⚡ EXECUTION METRICS\")\n    if icre.execution_log:\n        avg_time = sum(log['execution_time_seconds'] for log in icre.execution_log) / len(icre.execution_log)\n        print(f\"   Average Pipeline Time: {avg_time:.2f}s\")\n        print(f\"   Total Executions: {len(icre.execution_log)}\")\n    \n    # Emotion Analysis\n    print(f\"\\n🎭 EMOTION FREQUENCY ANALYSIS\")\n    emotion_history = memory_bank.get_emotion_history()\n    emotion_counts = defaultdict(int)\n    for emotion in emotion_history:\n        emotion_counts[emotion] += 1\n    \n    for emotion, count in sorted(emotion_counts.items(), key=lambda x: x[1], reverse=True)[:5]:\n        print(f\"   • {emotion}: {count} occurrences\")\n    \n    # A2A Communication Log\n    print(f\"\\n🔗 A2A PROTOCOL ACTIVITY\")\n    all_logs = (icre.agent1.session_log + icre.agent2.session_log + \n                icre.agent3.session_log + icre.agent4.session_log)\n    \n    for log in all_logs[-6:]:  # Show last 6 A2A messages\n        if 'a2a_message' in log:\n            print(f\"   [{log['agent']}] {log['a2a_message']}\")\n\ndisplay_metrics(icre)\n\n# ═══════════════════════════════════════════════════════════════════\n# STEP 12: CAPSTONE REQUIREMENTS VERIFICATION\n# ═══════════════════════════════════════════════════════════════════\n\nprint(\"\\n\" + \"=\"*70)\nprint(\"✅ CAPSTONE REQUIREMENTS VERIFICATION\")\nprint(\"=\"*70 + \"\\n\")\n\nrequirements = {\n    \"Multi-agent system\": \"✅ 4 agents (Parser, Pattern, Reflection, Clarity)\",\n    \"LLM agents\": \"✅ Google Gemini 1.5 Flash\",\n    \"Custom tools\": \"✅ Parsing tool, Pattern detection tool\",\n    \"Sequential agents\": \"✅ Agent1→Agent2→Agent3→Agent4 pipeline\",\n    \"Memory Bank\": \"✅ Long-term storage for patterns\",\n    \"Sessions & State\": \"✅ In-memory session management\",\n    \"A2A Protocol\": \"✅ Inter-agent communication logged\",\n    \"Observability\": \"✅ Logging, metrics, execution tracking\",\n    \"Context Engineering\": \"✅ Context compaction in Agent 2 & 3\"\n}\n\nfor requirement, status in requirements.items():\n    print(f\"{status} {requirement}\")\n\nprint(\"\\n\" + \"=\"*70)\nprint(\"🎉 ICRE SYSTEM FULLY OPERATIONAL\")\nprint(\"=\"*70)\nprint(\"\"\"\nNEXT STEPS FOR DEPLOYMENT:\n1. Create Gradio/Streamlit UI\n2. Add user authentication\n3. Deploy to Hugging Face Spaces or Cloud Run\n4. Prepare project writeup & video\n\nPROJECT COMPLETE ✓\n\"\"\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-30T18:39:50.409046Z","iopub.execute_input":"2025-11-30T18:39:50.40939Z","iopub.status.idle":"2025-11-30T18:41:10.391518Z","shell.execute_reply.started":"2025-11-30T18:39:50.409364Z","shell.execute_reply":"2025-11-30T18:41:10.390547Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# ═══════════════════════════════════════════════════════════════════\n# ICRE - Gradio Web Interface\n# Deploy this separately or add to end of main notebook\n# ═══════════════════════════════════════════════════════════════════\n\n\"\"\"\nBeautiful Gradio UI for ICRE - Inner Cognitive Reflection Engine\nAllows users to input thoughts and receive Clarity Maps\n\"\"\"\n\n!pip install gradio -q\n\nimport gradio as gr\nimport json\nfrom datetime import datetime\n\n# ═══════════════════════════════════════════════════════════════════\n# GRADIO INTERFACE FUNCTIONS\n# ═══════════════════════════════════════════════════════════════════\n\ndef process_thought_ui(raw_thought: str, show_details: bool = False):\n    \"\"\"\n    Process thought through ICRE pipeline and return formatted output\n    \"\"\"\n    \n    if not raw_thought or len(raw_thought.strip()) < 10:\n        return \"⚠️ Please enter a thought (at least 10 characters)\", \"\", \"\", \"\"\n    \n    try:\n        # Process through ICRE\n        clarity_map = icre.process_thought(raw_thought)\n        \n        # Format main output\n        main_output = f\"\"\"\n# 🗺️ Your Clarity Map\n\n## 🎯 Root Emotion\n**{clarity_map.root_emotion}**\n\n## ⚡ Core Conflict\n{clarity_map.conflict_summary}\n\n## 💭 Underlying Need\n{clarity_map.underlying_need}\n\n## 🧠 Cognitive Observation\n{clarity_map.full_reflection}\n\n---\n\n## 📊 Pattern Analysis\n{clarity_map.pattern_frequency}\n\n## 📈 Progress Indicator\n{clarity_map.progress_indicator}\n\"\"\"\n\n        if clarity_map.mental_loop:\n            main_output += f\"\\n## 🔄 Mental Loop Detected\\n{clarity_map.mental_loop}\\n\"\n        \n        # Stats output\n        stats = memory_bank.get_stats()\n        stats_output = f\"\"\"\n### 💾 Session Statistics\n- **Total Entries Processed**: {stats['total_entries']}\n- **Patterns Detected**: {stats['total_patterns']}\n- **Reflections Generated**: {stats['total_reflections']}\n- **Timestamp**: {clarity_map.timestamp}\n\"\"\"\n        \n        # Technical details (if requested)\n        technical_output = \"\"\n        if show_details:\n            # Get execution logs\n            recent_logs = icre.execution_log[-1] if icre.execution_log else {}\n            \n            technical_output = f\"\"\"\n### 🔧 Technical Details\n\n**Agent Pipeline Execution:**\n- Agent 1: Thought Parser ✓\n- Agent 2: Pattern Classifier ✓\n- Agent 3: Reflection Engine ✓\n- Agent 4: Clarity Structuring ✓\n\n**Execution Time:** {recent_logs.get('execution_time_seconds', 'N/A')}s\n\n**Entry ID:** {clarity_map.entry_id}\n\n**Memory Bank Status:**\n- Entries: {len(memory_bank.entries)}\n- Historical Context Used: Yes\n- A2A Protocol: Active\n\"\"\"\n        \n        # JSON export\n        json_output = json.dumps({\n            \"entry_id\": clarity_map.entry_id,\n            \"root_emotion\": clarity_map.root_emotion,\n            \"conflict_summary\": clarity_map.conflict_summary,\n            \"underlying_need\": clarity_map.underlying_need,\n            \"full_reflection\": clarity_map.full_reflection,\n            \"pattern_frequency\": clarity_map.pattern_frequency,\n            \"progress_indicator\": clarity_map.progress_indicator,\n            \"mental_loop\": clarity_map.mental_loop,\n            \"timestamp\": clarity_map.timestamp\n        }, indent=2)\n        \n        return main_output, stats_output, technical_output, json_output\n        \n    except Exception as e:\n        error_output = f\"\"\"\n# ❌ Processing Error\n\nAn error occurred while processing your thought:\n\n```\n{str(e)}\n```\n\n**Possible causes:**\n- API rate limit (wait 60 seconds)\n- Invalid API key\n- Network issue\n\n**Try again in a moment.**\n\"\"\"\n        return error_output, \"\", \"\", \"\"\n\ndef clear_memory():\n    \"\"\"Clear all memory and start fresh\"\"\"\n    global memory_bank, icre\n    memory_bank = MemoryBank()\n    icre = ICREOrchestrator(model, memory_bank)\n    return \"✅ Memory cleared. Starting fresh session.\"\n\ndef export_session_data():\n    \"\"\"Export all session data as JSON\"\"\"\n    data = {\n        \"session_timestamp\": datetime.now().isoformat(),\n        \"total_entries\": len(memory_bank.entries),\n        \"entries\": memory_bank.entries,\n        \"patterns\": memory_bank.patterns,\n        \"reflections\": memory_bank.reflections,\n        \"execution_log\": icre.execution_log\n    }\n    return json.dumps(data, indent=2)\n\n# ═══════════════════════════════════════════════════════════════════\n# GRADIO UI LAYOUT\n# ═══════════════════════════════════════════════════════════════════\n\n# Custom CSS for better styling\ncustom_css = \"\"\"\n.gradio-container {\n    font-family: 'Inter', sans-serif;\n}\n.main-header {\n    text-align: center;\n    padding: 20px;\n    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);\n    color: white;\n    border-radius: 10px;\n    margin-bottom: 20px;\n}\n\"\"\"\n\n# Create Gradio interface\nwith gr.Blocks(css=custom_css, title=\"ICRE - Inner Cognitive Reflection Engine\", theme=gr.themes.Soft()) as demo:\n    \n    # Header\n    gr.HTML(\"\"\"\n        <div class=\"main-header\">\n            <h1>🧠 ICRE - Inner Cognitive Reflection Engine</h1>\n            <p style='font-size: 18px; margin-top: 10px;'>\n                A Multi-Agent System for Deep Self-Understanding\n            </p>\n            <p style='font-size: 14px; opacity: 0.9;'>\n                Reflective Intelligence • No Advice • Pure Clarity\n            </p>\n        </div>\n    \"\"\")\n    \n    # Introduction\n    with gr.Accordion(\"ℹ️ What is ICRE?\", open=False):\n        gr.Markdown(\"\"\"\n        **ICRE (Inner Cognitive Reflection Engine)** is a multi-agent AI system that helps you understand your thoughts, \n        emotions, and cognitive patterns **without giving advice**.\n        \n        ### How it works:\n        1. **Agent 1 (Thought Parser)**: Extracts cognitive structure from your thoughts\n        2. **Agent 2 (Pattern Classifier)**: Detects recurring patterns using Memory Bank\n        3. **Agent 3 (Reflection Engine)**: Generates deep reflective insights\n        4. **Agent 4 (Clarity Structuring)**: Creates your personalized Clarity Map\n        \n        ### What makes ICRE unique:\n        - ✅ **Zero Advice**: Only reflection, never prescription\n        - ✅ **Pattern Detection**: Learns from your history\n        - ✅ **Multi-Agent Intelligence**: 4 specialized AI agents working together\n        - ✅ **Long-Term Memory**: Tracks patterns across sessions\n        \n        **Try it now:** Enter a thought you've been having and see your Clarity Map.\n        \"\"\")\n    \n    # Main input/output section\n    with gr.Row():\n        with gr.Column(scale=1):\n            gr.Markdown(\"### 💭 Enter Your Thought\")\n            \n            thought_input = gr.Textbox(\n                label=\"What's on your mind?\",\n                placeholder=\"Example: I've been feeling stuck at work. Everyone around me seems to be advancing, but I'm still doing the same tasks. Maybe I'm not good enough...\",\n                lines=8,\n                max_lines=15\n            )\n            \n            show_technical = gr.Checkbox(\n                label=\"Show technical details (agent execution logs)\",\n                value=False\n            )\n            \n            with gr.Row():\n                submit_btn = gr.Button(\"🧠 Generate Clarity Map\", variant=\"primary\", size=\"lg\")\n                clear_btn = gr.Button(\"🗑️ Clear Memory\", size=\"sm\")\n        \n        with gr.Column(scale=1):\n            gr.Markdown(\"### 🗺️ Your Clarity Map\")\n            \n            clarity_output = gr.Markdown(label=\"Clarity Map\")\n    \n    # Additional outputs (collapsible)\n    with gr.Row():\n        with gr.Column():\n            stats_output = gr.Markdown(label=\"Session Statistics\")\n        \n        with gr.Column():\n            technical_output = gr.Markdown(label=\"Technical Details\", visible=False)\n    \n    # JSON Export section\n    with gr.Accordion(\"📥 Export Data\", open=False):\n        gr.Markdown(\"### Export Individual Clarity Map\")\n        json_output = gr.Code(label=\"JSON Output\", language=\"json\")\n        \n        gr.Markdown(\"### Export Complete Session\")\n        session_export_btn = gr.Button(\"Export All Session Data\")\n        session_data = gr.Code(label=\"Complete Session Data\", language=\"json\")\n    \n    # Examples\n    with gr.Accordion(\"💡 Example Thoughts to Try\", open=False):\n        gr.Examples(\n            examples=[\n                [\"I've been working on this project for months and it feels like I'm spinning my wheels. Everyone around me seems to be getting promoted, but I'm stuck. Maybe I'm not good enough for this role.\"],\n                [\"Had a great conversation with a friend today. They really understood what I'm trying to do. But I noticed I was downplaying my achievements. Why do I do that? Maybe I don't believe I deserve success.\"],\n                [\"I keep comparing myself to others and feeling inadequate. Even when things go well, I find something to criticize about myself. Why can't I just accept that I'm doing okay?\"],\n                [\"I'm excited about this new opportunity, but also terrified. What if I fail? What if people see that I'm not as capable as they think I am?\"],\n            ],\n            inputs=thought_input,\n            label=\"Click any example to try it\"\n        )\n    \n    # Footer\n    gr.Markdown(\"\"\"\n    ---\n    \n    ### 🎓 About This Project\n    **ICRE** is a capstone project demonstrating:\n    - Multi-agent systems (4 sequential agents)\n    - LLM integration (Google Gemini)\n    - Memory Bank (long-term pattern storage)\n    - A2A Protocol (agent-to-agent communication)\n    - Context engineering & observability\n    \n    **Track:** Freestyle  \n    **Focus:** Reflective Intelligence without Advice  \n    **Technology:** Google Gemini Pro Latest, Pydantic, Python\n    \n    ---\n    \n    <div style='text-align: center; opacity: 0.7; font-size: 12px;'>\n        Built with ❤️ using Gradio | ICRE v1.0 | 2024\n    </div>\n    \"\"\")\n    \n    # Event handlers\n    submit_btn.click(\n        fn=process_thought_ui,\n        inputs=[thought_input, show_technical],\n        outputs=[clarity_output, stats_output, technical_output, json_output]\n    )\n    \n    clear_btn.click(\n        fn=clear_memory,\n        outputs=[clarity_output]\n    )\n    \n    session_export_btn.click(\n        fn=export_session_data,\n        outputs=[session_data]\n    )\n    \n    show_technical.change(\n        fn=lambda x: gr.update(visible=x),\n        inputs=[show_technical],\n        outputs=[technical_output]\n    )\n\n# ═══════════════════════════════════════════════════════════════════\n# LAUNCH GRADIO APP\n# ═══════════════════════════════════════════════════════════════════\n\nprint(\"\\n\" + \"=\"*70)\nprint(\"🚀 LAUNCHING ICRE WEB INTERFACE\")\nprint(\"=\"*70 + \"\\n\")\n\n# Launch with public URL for sharing\ndemo.launch(\n    share=True,  # Creates public URL\n    show_error=True,\n    server_name=\"0.0.0.0\",\n    server_port=7860\n)\n\nprint(\"\\n✅ ICRE Web Interface is now running!\")\nprint(\"📱 Access via the public URL above\")\nprint(\"🔗 Share this URL to let others try ICRE\\n\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-30T18:41:31.149822Z","iopub.execute_input":"2025-11-30T18:41:31.150135Z","iopub.status.idle":"2025-11-30T18:41:46.87253Z","shell.execute_reply.started":"2025-11-30T18:41:31.150107Z","shell.execute_reply":"2025-11-30T18:41:46.87153Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"import google.generativeai as genai\n\n# Configure\ngenai.configure(api_key=GOOGLE_API_KEY)\n\n# List available models\nprint(\"Available models:\")\nfor m in genai.list_models():\n    if 'generateContent' in m.supported_generation_methods:\n        print(f\"  ✓ {m.name}\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-29T17:49:59.883637Z","iopub.execute_input":"2025-11-29T17:49:59.884698Z","iopub.status.idle":"2025-11-29T17:49:59.944342Z","shell.execute_reply.started":"2025-11-29T17:49:59.884656Z","shell.execute_reply":"2025-11-29T17:49:59.943512Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}